[build]
  command = "npm run build"
  publish = ".next"

# Let Next.js handle routing. The previous /* -> /index.html redirect
# was likely intercepting image requests and breaking them in production.
